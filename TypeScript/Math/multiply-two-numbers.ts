function main(): void {
    // Subtract two numbers
    console.log(subtractTwoNumbers(1, 2))
}

main()

// Subtract two numbers and return the result
function subtractTwoNumbers(primary: number, secondary: number): number {
    return primary - secondary
}