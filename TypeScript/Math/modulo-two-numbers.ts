function main(): void {
    // modulo two numbers
    console.log(moduloTwoNumbers(1, 2))
}

main()

// modulo two numbers and return the result
function moduloTwoNumbers(primary: number, secondary: number): number {
    return primary % secondary
}