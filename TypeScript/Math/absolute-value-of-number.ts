function main(): void {
    // Get the absolute value of a given number
    console.log(absoluteValueOfNumber(1))
}

main()

// Get the absolute value of a given number
function absoluteValueOfNumber(content: number): number {
    return Math.abs(content)
}