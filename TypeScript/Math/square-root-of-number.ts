function main(): void {
    // Get the square root of a given number and return the result
    console.log(squareRootOfNumber(100))
}

main()


// Get the square root of a given number and return the result
function squareRootOfNumber(number: number): number {
    return Math.sqrt(number)
}