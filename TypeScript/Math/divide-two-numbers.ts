function main(): void {
    // Divide two numbers
    console.log(divideTwoNumbers(1, 2))
}

main()

// Divide two numbers and return the result
function divideTwoNumbers(primary: number, secondary: number): number {
    return primary / secondary
}