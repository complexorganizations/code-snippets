function main(): void {
    // Add two numbers
    console.log(addTwoNumbers(1, 2))
}

main()

// Add two numbers and return the result
function addTwoNumbers(primary: number, secondary: number): number {
    return primary + secondary
}