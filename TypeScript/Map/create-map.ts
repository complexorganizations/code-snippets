function main(): void {
    // Create a new map and add values to it.
    var randomMap: Map<string, string> = new Map<string, string>()
    // Add a key-value pair to the map, Print the map.
    console.log(randomMap.set("key3", "value3"))
}

main()