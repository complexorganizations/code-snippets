function main(): void {
    // Turn a string into lowercase and return it
    console.log(stringToLowerCase("Hello World"))
}

main()

// Turn a string into lowercase and return it
function stringToLowerCase(content: string): string {
    return content.toLowerCase()
}