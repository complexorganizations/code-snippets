function main(): void {
    // Turn the string into uppercase
    console.log(stringToUpperCase("Hello World"))
}

main()


// Turn a string into uppercase and return it
function stringToUpperCase(content: string): string {
    return content.toUpperCase()
}