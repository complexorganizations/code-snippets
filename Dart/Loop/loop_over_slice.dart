import "dart:core";

void main() {
    // Create a slice of strings
    var fruits = ["Apple", "Orange", "Pear"];
    // Loop over the slice
    for (var fruit in fruits) {
        print(fruit);
    }
}
