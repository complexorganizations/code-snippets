import "dart:core";

void main() {
    // Find the modulus of two numbers
    print(modulusOfTwoNumbers(10, 2));
}

// modulus two numbers and return the result
int modulusOfTwoNumbers(final int primary, final int secondary) {
    return primary % secondary;
}
