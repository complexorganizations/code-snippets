import "dart:core";

void main() {
  // Left shift value
  print(leftShiftValue(2, 2));
}

// Left shift the value.
int leftShiftValue(final int primary, final int secondary) {
  return primary << secondary;
}
