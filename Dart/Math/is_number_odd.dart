import "dart:core";

void main() {
  // To determine if a given number is odd.
  print(isNumberOdd(1));
}

// Determine whether a given integer is odd.
bool isNumberOdd(final int number) {
  return number.isOdd;
}
