import "dart:core";

void main() {
  // Multiply two numbers
  print(multiplyTwoNumbers(2, 3));
}

// multiply two numbers and return the sum.
int multiplyTwoNumbers(final int primary, final int secondary) {
  return primary * secondary;
}
