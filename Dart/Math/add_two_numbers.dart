import "dart:core";

void main() {
  // Add two numbers
  print(addTwoNumbers(1, 4));
}

// Add two numbers and than return the sum.
int addTwoNumbers(final int primary, final int secondary) {
  return primary + secondary;
}
