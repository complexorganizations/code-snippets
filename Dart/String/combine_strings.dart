import "dart:core";

void main() {
  // Combine multiple strings
  print(combineStrings("Hello", "World"));
}

// Combine multiple strings into one string.
String combineStrings(final String primary, final String secondary) {
  return primary + secondary;
}
