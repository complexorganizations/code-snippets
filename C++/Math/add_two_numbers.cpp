#include <iostream>

// Add two numbers and return the results.
int addTwoNumbers(int primary, int secondary) {
    return primary + secondary;
}

int main() {
    // Add two numbers and return the results.
    printf("%d \n", addTwoNumbers(1, 10));
    return 0;
}
