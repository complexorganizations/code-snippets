#include <iostream>

// Subtract two numbers and return the results.
int subtractTwoNumbers(int primary, int secondary) {
    return primary - secondary;
}

int main() {
    // Subtract two numbers and return the results.
    printf("%d \n", subtractTwoNumbers(10, 5));
    return 0;
}
