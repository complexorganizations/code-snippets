#include <iostream>

// Multiply two numbers and return the results.
int multiplyTwoNumbers(int primary, int secondary) {
    return primary * secondary;
}

int main() {
    // Multiply two numbers and return the results.
    printf("%d \n", multiplyTwoNumbers(2, 10));
    return 0;
}
