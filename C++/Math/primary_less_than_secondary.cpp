#include <iostream>

// Check if the primary value is less than the secondary value.
bool primaryLessThanSecondary(int primary, int secondary) {
    return primary < secondary;
}

int main() {
    // Check if the primary value is less than the secondary value.
    printf("%d \n", primaryLessThanSecondary(1, 10));
    return 0;
}
