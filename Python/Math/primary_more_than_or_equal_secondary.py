# Make that the primary value is greater than or equal to the secondary value.
def primary_more_than_or_equal_secondary(primary, secondary):
    return primary >= secondary


def main():
    # Make that the primary value is greater than or equal to the secondary value.
    print(primary_more_than_or_equal_secondary(10, 5))


main()
