# Check to see if the primary number is lower than the secondary number.
def primary_less_than_secondary(primary, secondary):
    return primary < secondary


def main():
    # Check to see if the primary number is lower than the secondary number.
    print(primary_less_than_secondary(5, 2))


main()
