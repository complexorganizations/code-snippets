# Get the array's first element.
def get_first_element_in_slice(provided_slice):
    return provided_slice[0]


def main():
    # Make a slice using a random set of data.
    randomSlice = ["c", "a", "b"]
    # Get the array's first element.
    print(get_first_element_in_slice(randomSlice))


main()
