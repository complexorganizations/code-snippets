fn main() {
    // Subtract two numbers
    println!("5 - 3 = {}", subtract_two_numbers(5, 3));
}

// Subtract two numbers and than return the difference.
fn subtract_two_numbers(primary: i32, secondary: i32) -> i32 {
    return primary - secondary;
}
