fn main() {
    // Multiply two numbers
    println!("3 * 2 = {}", multiply_two_numbers(3, 2));
}

// Multiply two numbers and than return the product.
fn multiply_two_numbers(primary: i32, secondary: i32) -> i32 {
    return primary * secondary;
}
